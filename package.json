{
  "name": "snapvid-backend",
  "version": "1.0.0",
  "private": true,
  "description": "SnapVid Media Downloader Backend",
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "dev:all": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend install && npm --prefix backend run build",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
